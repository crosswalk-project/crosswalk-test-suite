var step = '<font class="fontSize">'
            +'<p>Test Purpose: </p>'
            +'<p>Verifies the location information of latitude and longitude position can be fetched.</p>'
            +'<p>Test Pre-configuration: </p>'
            +'<p>Enable location service(via GPS, cellular network or wifi)</p>'
            +'<p>Expected Result: </p>'
            +'<p>Test passes if both the latitude and longitude return the correct position data.</p>'
          +'</font>';
