var step = '<p>Test Purpose: </p>'
          +'<p>Verifies the file can be showed and can write new content to a file</p>'
          +'<p>Test Steps: </p>'
          +'<ol>'
            +'<li>Click the \'select file\'</li>'
            +'<li>Click the \'write file\'</li>'
          +'</ol>'
          +'<p>Expected Result: </p>'
          +'<p>Test passes if can get the local file and write new content</p>';
