var step = '<font class="fontSize">'
            +'<p>Test Purpose: </p>'
            +'<p>Verifies AudioSystemContext interface is valid.</p>'
            +'<p>Test Steps: </p>'
            +'<ol>'
              +"<li>Click 'Connect' button, check if connect to audiosystem sucess and audiosystem information can be shown.</li>"
              +"<li>Click 'Disconnect' button, check if connection of audiosystem lost.</li>"
            +'</ol>'
            +'<p>Expected Result: </p>'
            +'<p>Test passes if connect and disconnect success, the inforamtion of audiosystem is correct.</p>'
          +'</font>';
