var step = '<font class="fontSize">'
            +'<p>Purpose:</p>'
            +'<p>Verify the app works well when two pages load each other for more than 512 times</p>'
            +'<p>Test Steps:</p>'
            +'<li>Click "Reload Bridge", wait for the counting over</li>'
            +'<p>Expected Result:</p>'
            +'<li>Test passes if the app does not crash and the page backs after clicking the button "Reload Bridge"</li>'
          +'</font>';
