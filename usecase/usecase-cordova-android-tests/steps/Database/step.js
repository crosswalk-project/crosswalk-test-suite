var step = '<font class="fontSize">'
            +'<p>Purpose:</p>'
            +'<p>Verify the localStorage or IndexDB or Web SQL Database is preserved when migrating from webview to Crosswalk</p>'
            +'<p>Test Steps</p>'
            +'<ol>'
            +'<li>Display "Save LocalStorage(or IndexDB/Web SQL Database) value successfully: " add input content in the box</li>'
            +'<li>Display "Get LocalStorage(or IndexDB/Web SQL Database) value: " add input content in the box</li>'
            +'</ol>'
          +'</font>';
