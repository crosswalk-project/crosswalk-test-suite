var step = '<p>Purpose:</p>'
            +'<p>Verify that the sample MobileSpec can build succeeded and work well on test device</p>'
            +'<p>Expected Results:</p>'
            +'<ol>'
            +'<li>Open the app named "Xcode" and display a project named "MobileSpec" on the left, include two subproject "Cordova.xcodeproj" and "XWalkView.xcodeproj"</li>'
            +'<li>Display message "Build Succeeded"</li>'
            +'<li>Launch the app named like "MobileSpec" successfully, no error message and no crash</li>'
            +'<li>Display message "MobileSpec" and "Apache Cordova Tests" after wait a moment</li>'
            +'</ol>';
