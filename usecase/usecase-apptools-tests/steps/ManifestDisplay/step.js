var step = '<p>Purpose:</p>'
            +'<p>Validate if web app can be launched with corresponding mode screen by manifest "display"</p>'
            +'<p>Expected Result:</p>'
            +'<ol>'
            +'<li>Test passes if all sub-tests pass</li>'
            +'</ol>';
