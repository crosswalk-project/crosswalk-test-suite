var step = '<p>Purpose:</p>'
            +'<p>Validate if web app can be launched with using the platform-specific convention for opening hyperlinks</p>'
            +'<p>Expected Result:</p>'
            +'<ol>'
            +'<li>Web app could be launched with using the platform-specific convention for opening hyperlinks</li>'
            +'</ol>';
