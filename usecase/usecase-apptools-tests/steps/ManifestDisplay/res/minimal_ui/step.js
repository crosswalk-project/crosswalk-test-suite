var step = '<p>Purpose:</p>'
            +'<p>Validate if web app can be launched with fullscreen and a minimal set of UI elements for controlling navigation</p>'
            +'<p>Expected Result:</p>'
            +'<ol>'
            +'<li>Web app could be launched with fullscreen and a minimal set of UI elements for controlling navigation</li>'
            +'</ol>';
