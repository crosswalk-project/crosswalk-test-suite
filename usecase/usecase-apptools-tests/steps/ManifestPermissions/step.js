var step = '<p>Purpose:</p>'
            +'<p>Validate if "xwalk_android_permissions" in manifest can work fine</p>'
            +'<p>Expected Result:</p>'
            +'<ol>'
            +'<li>Test passes if all sub-tests pass</li>'
            +'</ol>';
