var step = '<p>Purpose:</p>'
            +'<p>Validate if app can be started with default/custom icon splash screen</p>'
            +'<p>Expected Result:</p>'
            +'<ol>'
            +'<li>Test passes if all sub-tests pass</li>'
            +'</ol>';
