var step = '<p>Purpose:</p>'
            +'<p>Validate if versionName is from manifest version code</p>'
            +'<p>Expected Result:</p>'
            +'<ol>'
            +'<li>The app version is "0.0.1" and it is same with "xwalk_app_version" in manifest.json file</li>'
            +'</ol>';
