var step = '<p>Purpose:</p>'
            +'<p>Validate if app can be built when whitespace in path to apache ant</p>'
            +'<p>Expected Result:</p>'
            +'<ol>'
            +'<li>Success to build and launch the app</li>'
            +'</ol>';
