var step = '<p>Purpose:</p>'
            +'<p>Validate if Vendor is from the package ID by default instead and is changed by adding the xwalk_windows_vendor field to the web manifest manually</p>'
            +'<p>Expected Result:</p>'
            +'<ol>'
            +'<li>The "Publisher" column of "basicapp" app is "org.xwalk", and the "Publisher" column of "vendorcustom" app is "org.example.test"</li>'
            +'</ol>';
