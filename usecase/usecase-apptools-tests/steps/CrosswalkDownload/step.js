var step = '<p>Purpose:</p>'
            +'<p>Verifies if App Tool can download the crosswalk binary automatically</p>'
            +'<p>Expected Result:</p>'
            +'<ol>'
            +'<li>Display a progerss indication when downloading the crosswalk library</li>'
            +'</ol>';
