var step = '<p>Purpose:</p>'
            +'<p>Validate if web app could be launched with corresponding orientation</p>'
            +'<p>Expected Result:</p>'
            +'<ol>'
            +'<li>Test passes if all sub-tests pass</li>'
            +'</ol>';
