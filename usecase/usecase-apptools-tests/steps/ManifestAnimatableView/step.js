var step = '<p>Purpose:</p>'
            +'<p>Validate if app view can be scaled up and down when enabling animatable view</p>'
            +'<p>Expected Result:</p>'
            +'<ol>'
            +'<li>The view of "enable_animatableview" could be scaled up and down, and the view of "disable_animatableview" can not be scaled up and down</li>'
            +'</ol>';
