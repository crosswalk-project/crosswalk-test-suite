var step = '<p>Purpose:</p>'
            +'<p>Verifies if the size of basicapp and remotedebugging apk are nearly, and the size is less than 40M</p>'
            +'<p>Expected Result:</p>'
            +'<ol>'
            +'<li>The size of basicapp and remotedebugging apk are nearly, and the size is less than 40M</li>'
            +'</ol>';
