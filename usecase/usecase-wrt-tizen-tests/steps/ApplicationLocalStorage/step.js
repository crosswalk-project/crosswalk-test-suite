var step = '<font class="fontSize">'
            +'<p>Purpose:</p>'
            +'<p>Validate localStorage is exist when relaunch the app or relaunch the app after reboot the system</p>'
            +'<p>Expected Result:</p>'
            +'<ol>'
            +'<li>The webapp install successfully and launch successfully.</li>'
            +"<li>In the popup window, the textarea will output 'Save localStorage value successfully:...'</li>"
            +"<li>In the popup window, the textarea will output 'Get localStorage value:...'</li>"
            +'</ol>'
          +'</font>';
