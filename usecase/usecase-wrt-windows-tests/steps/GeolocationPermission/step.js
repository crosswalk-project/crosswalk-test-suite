var step = '<p>Purpose:</p>'
            +'<p>Verify that a dialog should pop-up asking the user whether he allows the application to use the API when a GeoLocation API is requested</p>'
            +'<p>Expected Result:</p>'
            +'<ol>'
            +'<li>Test pass if a dialog pop-up asking the user whether he allows the application to use the computer\'s location when you enter this sample page the first time</li>'
            +'</ol>';
