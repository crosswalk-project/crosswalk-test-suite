var step = '<p>Test Note: </p>'
            +'<p>Validate localStorage is exists when restart the app/reboot the system/upgrade the app</p>'
            +'<p>Test Purpose: </p>'
            +'<p>Validate localStorage is exists when restart the app/reboot the system/upgrade the app</p>'
            +'<p>Expected Result: </p>'
            +'<p>Test passes if all sub-tests pass.</p>';
