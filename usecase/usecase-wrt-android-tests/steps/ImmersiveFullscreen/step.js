var step = '<p>Purpose:</p>'
            +'<p>Verify that the immersive fullscreen mode can be recover after changing the volume on the device</p>'
            +'<p>Expected Result:</p>'
            +'<ol>'
            +'<li>Immersive Fullscreen is resumed(Android status bar and navigation bar hide)</li>'
            +'</ol>';
