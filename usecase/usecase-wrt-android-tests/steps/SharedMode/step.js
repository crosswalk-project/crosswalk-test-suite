var step = '<font class="fontSize">'
            +'<p>Purpose:</p>'
            +'<p>Verifies the app with shared mode, the app with embedded mode and the crosswalk runtime apk can be installed at the same time</p>'
            +'<p>Expected Result:</p>'
            +'<ol>'
            +'<li>Install the three apks successfully, no error message</li>'
            +'<li>Launch the two apks works without error, crashes as well</li>'
            +'</ol>'
          +'</font>';
