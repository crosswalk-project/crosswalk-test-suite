var step = '<p>Purpose:</p>'
            +'<p>Validate the crosswalk can work well after upgrading shared mode test app build using higher Crosswalk binary and runtime library when test with cookie</p>'
            +'<p>Expected Results:</p>'
            +'<ol>'
            +'<li>Launch the app successfully</li>'
            +'<li>Display "Cookie created"</li>'
            +'<li>Launch the app successfully</li>'
            +'<li>Display "The value of the cookie is " add input content in the box</li>'
            +'</ol>';
