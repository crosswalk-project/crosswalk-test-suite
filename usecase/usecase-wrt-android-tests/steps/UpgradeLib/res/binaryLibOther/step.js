var step = '<p>Purpose:</p>'
            +'<p>Validate the crosswalk can work well after upgrading shared mode test app build using higher Crosswalk binary and runtime library when test with localStorage/IndexDB/Web SQL Database</p>'
            +'<p>Expected Results:</p>'
            +'<ol>'
            +'<li>Launch the app successfully</li>'
            +'<li>Display "Save localStorage/IndexDB/Web SQL Database value successfully: " add input content in the box</li>'
            +'<li>Launch the app successfully</li>'
            +'<li>Display "Get localStorage/IndexDB/Web SQL Database value: " add input content in the box</li>'
            +'</ol>';
