var step = '<p>Purpose:</p>'
            +'<p>Verify the app can be back to home page when press "Back" key from "chrome://gpu" link</p>'
            +'<p>Expected Result:</p>'
            +'<ol>'
            +'<li>Go back to home page</li>'
            +'</ol>';
